#include<iostream>
#include<algorithm>
#include<math.h>
#include<string>
using namespace std;
int dp[100'001] = { 0, };

int main(void)
{
	string a, b, c;
	cin >> a >> b >> c;

	for (int i = 1; i <= a.size(); ++i)
	{
		for (int j = 1; j <= a.size(); ++j)
		{
			for (int k = 1; k <= a.size(); ++k)
			{

				
			}
		}
	}
	return 0;
}